language: php
php:
  - 7.1
before_script:
  - composer self-update
  - composer install
script:
  - vendor/bin/phpunit
jobs:
  include:
    - stage: release
      php: 7.2
      before_install:
        - nvm install 10
      before_script:
        - composer self-update
        - composer install
      script:
        - vendor/bin/phpunit
      after_success:
        - php vendor/bin/php-coveralls
