language: php
php:
  - 5.6
  - 7.0
  - 7.1
before_script:
  - composer self-update
  - composer install
script:
  - vendor/bin/phpunit
jobs:
  include:
    - stage: release
    php: "7.2"
    before_install:
      - nvm install 10
    before_script:
      - composer self-update
      - composer install
    script:
      - vendor/bin/phpunit
