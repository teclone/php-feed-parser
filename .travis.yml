language: php
php:
  - 7.1
before_script:
  - composer self-update
  - composer install
script:
  - vendor/bin/phpunit
jobs:
  include:
    - stage: release
      php: "7.2"
      before_script:
        - composer self-update
        - composer install
      script:
        - vendor/bin/phpunit
